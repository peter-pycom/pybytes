ntp?

ischarging

accelerometer wake (backoff)
active mode (no acc wake, frequent wakeups)
idle mode (acc, rare)
activity kpi

gps distance since last
cvs log of sensors
create gps track send hourly link (json)
gps time

uptime with pic sleep based on planned sleeptime

pygate network speed
